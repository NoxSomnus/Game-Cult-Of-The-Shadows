using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AuraTransition : MonoBehaviour
{
    // Start is called before the first frame update
    public void DisableAura() 
    {
        gameObject.SetActive(false);
    }

}
